* service의 목적

mapper 인터페이스 구현 
> vo, xml로 컨트롤러에서 사용 그대로 해도 됨

그러나 쿼리문을 한번만 쓰고 안쓰진 않음

ex) 결제 후 포인트 적립
결제 > 상품 테이블에서 해당 제고 제거 >> 업데이트
포인트 적립 > 회원테이블 포인트 수정 >> 업데이트

>> mapper만 두고 컨트롤러에서 직접 쓴다면 쿼리문 두개를 직접 때려주어야 함

>> 쿼리를 더 많이 쓴다면...??? 
>> 에러가 더 많이남

쿼리문을 사용하는 service를 거쳐서 서비스의 메소드 하나로 쿼리문을 쓸 수 있도록 ! 
mapper > 쿼리 1개당 매퍼 하나 ! 

서비스 : 여러개 매퍼 사용 메서드 작성
컨트롤러 : 서비스 실행

service - service interface

* interface>> 재사용 !
* 연결을 느슨하게 해주기 위해 (결합 느슨)


